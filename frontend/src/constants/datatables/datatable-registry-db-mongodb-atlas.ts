import type { DataTableDefinition } from './types';

// Allocated ID range: DBM30701-DBM30704
export const DBM_MONGODB_ATLAS_DATATABLES: DataTableDefinition[] = [
  // TODO: Add MongoDB Atlas datatable definitions (ID range: DBM30701-DBM30704)
];
