/**
 * Mock Data — PII Data Masking
 *
 * 9 policies aligned with global compliance frameworks:
 *   1. General PII Baseline          — email, phone, SSN, IP address
 *   2. Financial Data Protection      — credit card, SSN hash
 *   3. Secrets & Credentials          — JWT, API keys, AWS, private keys
 *   4. URL Credentials Scrubber       — embedded creds in URLs
 *   5. GDPR – EU Personal Data        — IBAN, national IDs, DOB, passport
 *   6. HIPAA – Healthcare PII         — MRN, NPI, health insurance, DEA
 *   7. PCI-DSS – Payment Card Data    — CVV, expiry, routing/account numbers
 *   8. SOC 2 / ISO 27001 – SaaS Keys  — GitHub, Slack, Stripe, Azure, GCP
 *   9. CCPA – CA Consumer Privacy     — driver's license, VIN, biometrics
 */

import type { MaskingPolicy } from '@/types/data-masking'
import {
  TargetField,
  MatchType,
  MaskType,
  BuiltinPattern,
} from '@/types/data-masking'

// ─── Helpers ─────────────────────────────────────────────────────────────────
const daysAgo = (n: number) => new Date(Date.now() - n * 86_400_000).toISOString()

// ─── Mock Policies ─────────────────────────────────────────────────────────────

export const MOCK_MASKING_POLICIES: MaskingPolicy[] = [
  // ── 1. General PII Baseline ──────────────────────────────────────────────
  {
    id: 'dm-policy-001',
    organizationId: 'org-001',
    workspaceId: null,
    name: 'General PII Baseline',
    description: 'Redacts email addresses and phone numbers from all log fields. Suitable as a first-level privacy layer for any environment.',
    enabled: true,
    rules: [
      {
        id: 'rule-gen-001',
        name: 'Redact email addresses',
        description: 'Matches and redacts RFC-5322 email addresses in all telemetry fields',
        enabled: true,
        priority: 10,
        targetField: TargetField.ALL,
        matchType: MatchType.BUILTIN,
        builtinPattern: BuiltinPattern.EMAIL,
        maskType: MaskType.REPLACE,
        replacement: '[EMAIL]',
      },
      {
        id: 'rule-gen-002',
        name: 'Redact phone numbers',
        description: 'Matches North-American and international phone number formats',
        enabled: true,
        priority: 20,
        targetField: TargetField.ALL,
        matchType: MatchType.BUILTIN,
        builtinPattern: BuiltinPattern.PHONE,
        maskType: MaskType.REPLACE,
        replacement: '[PHONE]',
      },
      {
        id: 'rule-gen-003',
        name: 'Anonymize IP addresses',
        description: 'Replaces IPv4 addresses with a fixed token to prevent geolocation tracking',
        enabled: true,
        priority: 30,
        targetField: TargetField.ALL,
        matchType: MatchType.BUILTIN,
        builtinPattern: BuiltinPattern.IP_ADDRESS,
        maskType: MaskType.REPLACE,
        replacement: '[IP]',
      },
    ],
    ruleCount: 3,
    activeRuleCount: 3,
    createdBy: 'system-seed',
    updatedBy: null,
    createdAt: daysAgo(30),
    updatedAt: daysAgo(30),
  },

  // ── 2. Financial Data Protection ─────────────────────────────────────────
  {
    id: 'dm-policy-002',
    organizationId: 'org-001',
    workspaceId: null,
    name: 'Financial Data Protection',
    description: 'Hashes credit card numbers and US Social Security Numbers using SHA-256 for correlation without exposure. Meets PCI-DSS baseline requirements.',
    enabled: true,
    rules: [
      {
        id: 'rule-fin-001',
        name: 'Hash credit card numbers',
        description: 'Applies SHA-256 hash to Visa, Mastercard, Amex, Discover, and JCB PANs',
        enabled: true,
        priority: 10,
        targetField: TargetField.BODY,
        matchType: MatchType.BUILTIN,
        builtinPattern: BuiltinPattern.CREDIT_CARD,
        maskType: MaskType.HASH,
      },
      {
        id: 'rule-fin-002',
        name: 'Hash Social Security Numbers',
        description: 'SHA-256 hash of US SSN in ddd-dd-dddd format',
        enabled: true,
        priority: 20,
        targetField: TargetField.ALL,
        matchType: MatchType.BUILTIN,
        builtinPattern: BuiltinPattern.SSN,
        maskType: MaskType.HASH,
      },
    ],
    ruleCount: 2,
    activeRuleCount: 2,
    createdBy: 'system-seed',
    updatedBy: null,
    createdAt: daysAgo(28),
    updatedAt: daysAgo(28),
  },

  // ── 3. Secrets & Credentials ─────────────────────────────────────────────
  {
    id: 'dm-policy-003',
    organizationId: 'org-001',
    workspaceId: null,
    name: 'Secrets & Credentials Masking',
    description: 'Redacts JWT tokens, generic API keys, AWS access keys, and PEM private key material from all telemetry. Essential for preventing secret leakage via observability pipelines.',
    enabled: true,
    rules: [
      {
        id: 'rule-sec-001',
        name: 'Redact JWT tokens',
        enabled: true,
        priority: 10,
        targetField: TargetField.ALL,
        matchType: MatchType.BUILTIN,
        builtinPattern: BuiltinPattern.JWT_TOKEN,
        maskType: MaskType.REDACT,
        replacement: '[JWT_REDACTED]',
      },
      {
        id: 'rule-sec-002',
        name: 'Redact generic API keys',
        enabled: true,
        priority: 20,
        targetField: TargetField.ALL,
        matchType: MatchType.BUILTIN,
        builtinPattern: BuiltinPattern.API_KEY_GENERIC,
        maskType: MaskType.REDACT,
        replacement: '[API_KEY]',
      },
      {
        id: 'rule-sec-003',
        name: 'Redact AWS access keys',
        enabled: true,
        priority: 30,
        targetField: TargetField.ALL,
        matchType: MatchType.BUILTIN,
        builtinPattern: BuiltinPattern.AWS_ACCESS_KEY,
        maskType: MaskType.REDACT,
        replacement: '[AWS_KEY]',
      },
      {
        id: 'rule-sec-004',
        name: 'Redact PEM private key material',
        enabled: true,
        priority: 40,
        targetField: TargetField.BODY,
        matchType: MatchType.BUILTIN,
        builtinPattern: BuiltinPattern.PRIVATE_KEY,
        maskType: MaskType.REDACT,
        replacement: '[PRIVATE_KEY]',
      },
    ],
    ruleCount: 4,
    activeRuleCount: 4,
    createdBy: 'system-seed',
    updatedBy: null,
    createdAt: daysAgo(25),
    updatedAt: daysAgo(25),
  },

  // ── 4. URL Credentials Scrubber ───────────────────────────────────────────
  {
    id: 'dm-policy-004',
    organizationId: 'org-001',
    workspaceId: null,
    name: 'URL Credentials Scrubber',
    description: 'Strips embedded credentials (user:password@host) from connection string URLs in resource attributes. Enable when applications log database or broker URLs.',
    enabled: false,
    rules: [
      {
        id: 'rule-url-001',
        name: 'Remove embedded URL credentials',
        enabled: true,
        priority: 10,
        targetField: TargetField.RESOURCE_ATTRIBUTES,
        matchType: MatchType.BUILTIN,
        builtinPattern: BuiltinPattern.URL_CREDENTIALS,
        maskType: MaskType.REDACT,
        replacement: '[CREDS_REMOVED]',
      },
    ],
    ruleCount: 1,
    activeRuleCount: 1,
    createdBy: 'system-seed',
    updatedBy: null,
    createdAt: daysAgo(20),
    updatedAt: daysAgo(20),
  },

  // ── 5. GDPR – EU Personal Data ────────────────────────────────────────────
  {
    id: 'dm-policy-005',
    organizationId: 'org-001',
    workspaceId: null,
    name: 'GDPR – EU Personal Data Protection',
    description: 'Masks EU personal identifiers required under GDPR Art. 4 & 9: IBAN bank accounts, passport numbers, date of birth, and national IDs. Enables data minimization at ingestion time.',
    enabled: true,
    rules: [
      {
        id: 'rule-gdpr-001',
        name: 'Hash IBAN bank account numbers',
        description: 'SHA-256 hash of International Bank Account Numbers (ISO 13616)',
        enabled: true,
        priority: 10,
        targetField: TargetField.ALL,
        matchType: MatchType.REGEX,
        customPattern: '[A-Z]{2}[0-9]{2}[A-Z0-9]{4}[0-9]{7}(?:[A-Z0-9]{0,16})',
        maskType: MaskType.HASH,
      },
      {
        id: 'rule-gdpr-002',
        name: 'Redact passport numbers',
        description: 'Context-aware: redacts passport numbers preceded by "passport" keyword',
        enabled: true,
        priority: 20,
        targetField: TargetField.ALL,
        matchType: MatchType.REGEX,
        customPattern: '(?:passport|pass[-_\\s]?(?:no|nr|num|number))\\s*[:\\-=]?\\s*[A-Z0-9]{7,9}',
        maskType: MaskType.REDACT,
        replacement: '[PASSPORT]',
      },
      {
        id: 'rule-gdpr-003',
        name: 'Redact date of birth',
        description: 'Detects DOB in context: "dob:", "date_of_birth:", "birthdate:" followed by common date formats',
        enabled: true,
        priority: 30,
        targetField: TargetField.ALL,
        matchType: MatchType.REGEX,
        customPattern: '(?:dob|date[-_\\s]?of[-_\\s]?birth|birth[-_\\s]?date)\\s*[:\\-=]?\\s*(?:\\d{1,2}[/\\-.\\s]\\d{1,2}[/\\-.\\s]\\d{2,4}|\\d{4}[/\\-.]\\d{1,2}[/\\-.]\\d{1,2})',
        maskType: MaskType.REDACT,
        replacement: '[DOB]',
      },
      {
        id: 'rule-gdpr-004',
        name: 'Redact SWIFT/BIC codes',
        description: 'Matches 8-11 character SWIFT/BIC bank codes (ISO 9362)',
        enabled: true,
        priority: 40,
        targetField: TargetField.ALL,
        matchType: MatchType.REGEX,
        customPattern: '\\b[A-Z]{6}[A-Z0-9]{2}(?:[A-Z0-9]{3})?\\b',
        maskType: MaskType.REDACT,
        replacement: '[BIC]',
      },
      {
        id: 'rule-gdpr-005',
        name: 'Anonymize IP addresses (GDPR recital 49)',
        description: 'Replaces IPv4/IPv6 addresses — IP is personal data under GDPR',
        enabled: true,
        priority: 50,
        targetField: TargetField.ALL,
        matchType: MatchType.BUILTIN,
        builtinPattern: BuiltinPattern.IP_ADDRESS,
        maskType: MaskType.REPLACE,
        replacement: '[IP_ANONYMIZED]',
      },
    ],
    ruleCount: 5,
    activeRuleCount: 5,
    createdBy: 'system-seed',
    updatedBy: null,
    createdAt: daysAgo(15),
    updatedAt: daysAgo(15),
  },

  // ── 6. HIPAA – Healthcare PII ─────────────────────────────────────────────
  {
    id: 'dm-policy-006',
    organizationId: 'org-001',
    workspaceId: null,
    name: 'HIPAA – Healthcare Data Compliance',
    description: 'Covers HIPAA Safe Harbor identifiers (45 CFR §164.514): Medical Record Numbers, National Provider Identifiers, health insurance member IDs, and DEA numbers. Enable for healthcare workloads.',
    enabled: false,
    rules: [
      {
        id: 'rule-hipaa-001',
        name: 'Redact Medical Record Numbers (MRN)',
        description: 'Detects MRN context: "mrn:", "medical_record_id:" etc.',
        enabled: true,
        priority: 10,
        targetField: TargetField.ALL,
        matchType: MatchType.REGEX,
        customPattern: '\\b(?:MRN|medical[-_\\s]?record(?:[-_\\s]?(?:no|num|id|number))?)\\s*[:\\-=]?\\s*[A-Z0-9]{5,15}\\b',
        maskType: MaskType.REDACT,
        replacement: '[MRN]',
      },
      {
        id: 'rule-hipaa-002',
        name: 'Redact National Provider Identifiers (NPI)',
        description: 'US NPI is a 10-digit unique healthcare provider ID',
        enabled: true,
        priority: 20,
        targetField: TargetField.ALL,
        matchType: MatchType.REGEX,
        customPattern: '\\b(?:NPI|national[-_\\s]?provider(?:[-_\\s]?(?:id|identifier|no|num))?)\\s*[:\\-=]?\\s*\\d{10}\\b',
        maskType: MaskType.REDACT,
        replacement: '[NPI]',
      },
      {
        id: 'rule-hipaa-003',
        name: 'Redact health insurance member IDs',
        description: 'Matches member_id, insurance_id, policy_no patterns common in healthcare APIs',
        enabled: true,
        priority: 30,
        targetField: TargetField.ALL,
        matchType: MatchType.REGEX,
        customPattern: '\\b(?:member[-_\\s]?id|insurance[-_\\s]?(?:id|no|num)|policy[-_\\s]?(?:no|num|id))\\s*[:\\-=]?\\s*[A-Z0-9\\-]{8,15}\\b',
        maskType: MaskType.REDACT,
        replacement: '[MEMBER_ID]',
      },
      {
        id: 'rule-hipaa-004',
        name: 'Redact DEA registration numbers',
        description: 'US Drug Enforcement Agency practitioner numbers (2 letters + 7 digits)',
        enabled: true,
        priority: 40,
        targetField: TargetField.ALL,
        matchType: MatchType.REGEX,
        customPattern: '\\b(?:DEA(?:[-_\\s]?(?:no|num|number))?)\\s*[:\\-=]?\\s*[A-Z]{2}[0-9]{7}\\b',
        maskType: MaskType.REDACT,
        replacement: '[DEA]',
      },
      {
        id: 'rule-hipaa-005',
        name: 'Redact date of birth (PHI)',
        description: 'DOB is one of the 18 PHI identifiers under HIPAA Safe Harbor',
        enabled: true,
        priority: 50,
        targetField: TargetField.ALL,
        matchType: MatchType.REGEX,
        customPattern: '(?:dob|date[-_\\s]?of[-_\\s]?birth|birth[-_\\s]?date|patient[-_\\s]?dob)\\s*[:\\-=]?\\s*(?:\\d{1,2}[/\\-.\\s]\\d{1,2}[/\\-.\\s]\\d{2,4}|\\d{4}[/\\-.]\\d{1,2}[/\\-.]\\d{1,2})',
        maskType: MaskType.REDACT,
        replacement: '[DOB]',
      },
    ],
    ruleCount: 5,
    activeRuleCount: 5,
    createdBy: 'system-seed',
    updatedBy: null,
    createdAt: daysAgo(12),
    updatedAt: daysAgo(12),
  },

  // ── 7. PCI-DSS – Payment Card ─────────────────────────────────────────────
  {
    id: 'dm-policy-007',
    organizationId: 'org-001',
    workspaceId: null,
    name: 'PCI-DSS – Payment Card Data Security',
    description: 'Protects cardholder data (CHD) per PCI-DSS v4.0: CVV/CVC codes, card expiry dates, bank routing numbers, and account numbers. Required for any environment handling payment transactions.',
    enabled: true,
    rules: [
      {
        id: 'rule-pci-001',
        name: 'Redact CVV/CVC security codes',
        description: 'PCI-DSS 3.3 prohibits storing CVV/CVC. Matches labelled security code patterns.',
        enabled: true,
        priority: 10,
        targetField: TargetField.ALL,
        matchType: MatchType.REGEX,
        customPattern: '\\b(?:cvv|cvc|cvv2|cvc2|cid|security[-_\\s]?code)\\s*[:\\-=]?\\s*[0-9]{3,4}\\b',
        maskType: MaskType.REDACT,
        replacement: '[CVV]',
      },
      {
        id: 'rule-pci-002',
        name: 'Redact card expiry dates',
        description: 'Detects expiry dates in MM/YY, MM-YY, or MM/YYYY formats when labelled',
        enabled: true,
        priority: 20,
        targetField: TargetField.ALL,
        matchType: MatchType.REGEX,
        customPattern: '(?:exp(?:ir(?:y|ation)?)?[-_\\s]?(?:date)?|valid[-_\\s]?(?:thru|through|until)|card[-_\\s]?exp)\\s*[:\\-=]?\\s*(?:0[1-9]|1[0-2])\\s*[/\\-]\\s*(?:[0-9]{2,4})',
        maskType: MaskType.REDACT,
        replacement: '[EXPIRY]',
      },
      {
        id: 'rule-pci-003',
        name: 'Hash bank routing numbers (ABA)',
        description: 'US ABA routing numbers are 9 digits — hashed for correlation tracing',
        enabled: true,
        priority: 30,
        targetField: TargetField.ALL,
        matchType: MatchType.REGEX,
        customPattern: '\\b(?:routing|aba|transit|routing[-_\\s]?(?:no|num|number))\\s*[:\\-=]?\\s*[0-9]{9}\\b',
        maskType: MaskType.HASH,
      },
      {
        id: 'rule-pci-004',
        name: 'Hash bank account numbers',
        description: 'Matches labelled bank account patterns (8–17 digits)',
        enabled: true,
        priority: 40,
        targetField: TargetField.ALL,
        matchType: MatchType.REGEX,
        customPattern: '\\b(?:bank[-_\\s]?acct|bank[-_\\s]?account|account[-_\\s]?(?:no|num|number)|acct[-_\\s]?(?:no|num))\\s*[:\\-=]?\\s*[0-9]{8,17}\\b',
        maskType: MaskType.HASH,
      },
    ],
    ruleCount: 4,
    activeRuleCount: 4,
    createdBy: 'system-seed',
    updatedBy: null,
    createdAt: daysAgo(10),
    updatedAt: daysAgo(10),
  },

  // ── 8. SOC 2 / ISO 27001 – SaaS Secrets ──────────────────────────────────
  {
    id: 'dm-policy-008',
    organizationId: 'org-001',
    workspaceId: null,
    name: 'SOC 2 / ISO 27001 – SaaS Platform Secrets',
    description: 'Redacts third-party SaaS API tokens that may appear in service logs: GitHub PATs, Slack tokens, Stripe keys, SendGrid keys, Twilio SIDs, and GCP service account credentials. Supports SOC 2 CC6.1 and ISO 27001 A.9.4.',
    enabled: true,
    rules: [
      {
        id: 'rule-soc-001',
        name: 'Redact GitHub personal access tokens',
        description: 'Matches GitHub PAT prefixes: ghp_, gho_, ghu_, ghs_, ghr_, github_pat_',
        enabled: true,
        priority: 10,
        targetField: TargetField.ALL,
        matchType: MatchType.REGEX,
        customPattern: '\\b(?:ghp|gho|ghu|ghs|ghr)_[A-Za-z0-9_]{36,255}\\b|github_pat_[A-Za-z0-9_]{82}',
        maskType: MaskType.REDACT,
        replacement: '[GITHUB_TOKEN]',
      },
      {
        id: 'rule-soc-002',
        name: 'Redact Slack API tokens',
        description: 'Matches xoxb- (bot), xoxa- (app), xoxp- (user), xoxs- (workspace) Slack tokens',
        enabled: true,
        priority: 20,
        targetField: TargetField.ALL,
        matchType: MatchType.REGEX,
        customPattern: 'xox[baprs]-[0-9A-Za-z\\-]{16,}',
        maskType: MaskType.REDACT,
        replacement: '[SLACK_TOKEN]',
      },
      {
        id: 'rule-soc-003',
        name: 'Redact Stripe API keys',
        description: 'Matches Stripe live and test secret/publishable/restricted keys',
        enabled: true,
        priority: 30,
        targetField: TargetField.ALL,
        matchType: MatchType.REGEX,
        customPattern: '\\b(?:sk_live|sk_test|pk_live|pk_test|rk_live|rk_test)_[A-Za-z0-9]{24,}\\b',
        maskType: MaskType.REDACT,
        replacement: '[STRIPE_KEY]',
      },
      {
        id: 'rule-soc-004',
        name: 'Redact SendGrid API keys',
        description: 'SendGrid keys follow the pattern SG.<22chars>.<43chars>',
        enabled: true,
        priority: 40,
        targetField: TargetField.ALL,
        matchType: MatchType.REGEX,
        customPattern: 'SG\\.[A-Za-z0-9_\\-]{22,}\\.[A-Za-z0-9_\\-]{43,}',
        maskType: MaskType.REDACT,
        replacement: '[SENDGRID_KEY]',
      },
      {
        id: 'rule-soc-005',
        name: 'Redact Twilio Account SIDs',
        description: 'Twilio Account SIDs start with AC followed by 32 hex chars',
        enabled: true,
        priority: 50,
        targetField: TargetField.ALL,
        matchType: MatchType.REGEX,
        customPattern: '\\bAC[a-f0-9]{32}\\b',
        maskType: MaskType.REDACT,
        replacement: '[TWILIO_SID]',
      },
      {
        id: 'rule-soc-006',
        name: 'Redact GCP service account credentials',
        description: 'Detects private_key fields in GCP service account JSON blobs in logs',
        enabled: true,
        priority: 60,
        targetField: TargetField.BODY,
        matchType: MatchType.REGEX,
        customPattern: '"private_key"\\s*:\\s*"-----BEGIN[^"]*"',
        maskType: MaskType.REDACT,
        replacement: '"private_key":"[GCP_KEY_REDACTED]"',
      },
    ],
    ruleCount: 6,
    activeRuleCount: 6,
    createdBy: 'system-seed',
    updatedBy: null,
    createdAt: daysAgo(8),
    updatedAt: daysAgo(8),
  },

  // ── 9. CCPA – California Consumer Privacy ────────────────────────────────
  {
    id: 'dm-policy-009',
    organizationId: 'org-001',
    workspaceId: null,
    name: 'CCPA – California Consumer Privacy Act',
    description: 'Protects California consumer personal information under Cal. Civ. Code §1798.140: driver\'s license numbers, Vehicle Identification Numbers (VIN), and biometric identifier references. Enable for California-resident user workloads.',
    enabled: false,
    rules: [
      {
        id: 'rule-ccpa-001',
        name: 'Redact California driver\'s license numbers',
        description: 'CA DL format: 1 letter + 7 digits (e.g., D1234567)',
        enabled: true,
        priority: 10,
        targetField: TargetField.ALL,
        matchType: MatchType.REGEX,
        customPattern: '(?:dl|driv(?:er)?s?[-_\\s]?lic(?:ense)?)\\s*[:\\-=]?\\s*[A-Z][0-9]{7}\\b',
        maskType: MaskType.REDACT,
        replacement: '[DL]',
      },
      {
        id: 'rule-ccpa-002',
        name: 'Redact Vehicle Identification Numbers (VIN)',
        description: '17-char ISO 3779 VIN — unique vehicle identifier that is personal data under CCPA',
        enabled: true,
        priority: 20,
        targetField: TargetField.ALL,
        matchType: MatchType.REGEX,
        customPattern: '\\b[A-HJ-NPR-Z0-9]{17}\\b',
        maskType: MaskType.REDACT,
        replacement: '[VIN]',
      },
      {
        id: 'rule-ccpa-003',
        name: 'Redact biometric data references',
        description: 'Detects base64-encoded biometric payloads labelled with fingerprint/facial/iris keywords',
        enabled: true,
        priority: 30,
        targetField: TargetField.BODY,
        matchType: MatchType.REGEX,
        customPattern: '(?:biometric|fingerprint|facial[-_\\s]?recognition|retina|iris[-_\\s]?scan)\\s*[:\\-=]?\\s*[A-Za-z0-9+/=]{20,}',
        maskType: MaskType.REDACT,
        replacement: '[BIOMETRIC]',
      },
    ],
    ruleCount: 3,
    activeRuleCount: 3,
    createdBy: 'system-seed',
    updatedBy: null,
    createdAt: daysAgo(5),
    updatedAt: daysAgo(5),
  },
]

// ─── Unified mock service ──────────────────────────────────────────────────────

export const dataMaskingMock = {
  policies: MOCK_MASKING_POLICIES,
}

export default dataMaskingMock
